import React from 'react';
import Main from "./Main";
import {Header} from "../global/header";

const Home = (props) => {
    return (
        <div>
            <Header/>
            <Main/>
        </div>
    );
};

export default Home;
